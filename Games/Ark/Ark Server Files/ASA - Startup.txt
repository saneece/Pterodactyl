rmv() { echo "stopping server"; rcon -t rcon -a 127.0.0.1:${RCON_PORT} -p ${ARK_ADMIN_PASSWORD} KeepAlive && rcon -t rcon -a 127.0.0.1:${RCON_PORT} -p ${ARK_ADMIN_PASSWORD} DoExit && wait ${ARK_PID}; echo "Server Closed"; exit; }; trap rmv 15 2; proton run ./ShooterGame/Binaries/Win64/ArkAscendedServer.exe TheIsland_WP?listen?MaxPlayers=70?SessionName=\"TrepIsland\"?Port=50109?QueryPort=50111?RCONPort=50112?RCONEnabled=True$( [ "$SERVER_PVE" == "0" ] || printf %s '?ServerPVE=True' )?ServerPassword="KingdomIsland"?ServerAdminPassword="KingdomIslandAdmin" -WinLiveMaxPlayers=70 -oldconsole -servergamelog $( [ -z "$MOD_IDS" ] || printf %s ' -mods=' $MOD_IDS )$( [ "$BATTLE_EYE" == "1" ] || printf %s ' -NoBattlEye' ) -Port=50109 -crossplay & ARK_PID=$! ; tail -c0 -F ./ShooterGame/Saved/Logs/ShooterGame.log --pid=$ARK_PID & until echo "waiting for rcon connection..."; (rcon -t rcon -a 127.0.0.1:${RCON_PORT} -p ${ARK_ADMIN_PASSWORD})<&0 & wait $!; do sleep 5; done
